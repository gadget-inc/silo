pub mod factory;
pub mod grpc;
pub mod job_store_shard;
pub mod keys;
pub mod membership;
pub mod retry;
pub mod settings;
pub mod storage;
pub mod pb {
    // Generated by prost/tonic into OUT_DIR (per prost-build best practice)
    include!(concat!(env!("OUT_DIR"), "/silo.v1.rs"));
}
pub mod bootstrap;
pub mod server;
pub mod ring;

pub mod membershippb {
    include!(concat!(env!("OUT_DIR"), "/silo.membership.v1.rs"));
}

// Temporary stub module to satisfy main.rs during refactor
pub mod raft {
    use crate::settings::RaftConfig;
    pub struct OpenRaftNode;
    impl OpenRaftNode {
        pub async fn start(_cfg: RaftConfig) -> anyhow::Result<()> {
            Ok(())
        }
    }
}
